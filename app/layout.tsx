import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "University of Calcutta - Qiskit Fall Fest 2026",
  description: "Qiskit Fall Fest is a global series of quantum computing events supported by IBM Quantum and organized by student leaders worldwide. Hosted at the University of Calcutta, our mission is to build, educate, and empower the next generation of quantum researchers, developers, and enthusiasts.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
